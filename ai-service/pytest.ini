[pytest]
; Ensures `app` is always importable when running tests, regardless
; of whether you invoke `pytest` directly or via `python -m pytest`
; (these two behave differently: `python -m pytest` auto-adds the
; current directory to sys.path, but bare `pytest` does not — this
; setting makes both work the same way).
pythonpath = .
